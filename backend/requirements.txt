google-auth==2.35.0
google-auth-oauthlib==1.2.1
# requests/python-dotenv pinned to versions with no known vulnerabilities
# per pip-audit (5.7) as of this pin — bump the pin (and re-run
# pip-audit) if a new CVE lands on these, don't just silence the
# finding. requests is pinned to 2.34.2 specifically (not the lower
# 2.33.0 that alone would clear pip-audit) because prawcore (praw's
# dependency, see the Reddit adapter below) requires requests>=2.34.2.
requests==2.34.2
python-dotenv==1.2.2

# Phase 2 foundations (backend/app/) — schema, persistence, service config.
# See docs/decisions/persistence-choice.md for why Postgres + SQLAlchemy + Alembic.
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic-settings==2.6.1

# Phase 4/5 adapters and auth primitives (backend/app/jobs/, backend/app/auth.py)
praw==8.0.3          # Reddit adapter (4.3) — see fetch_reddit_mentions.py
bcrypt==4.2.1        # password hashing for auth primitives (5.5) — see app/auth.py

# Phase 6/7 classification and API layer
# anthropic 1.x is built on httpx2, not the separate `httpx` PyPI package —
# see app/classification.py / docs/decisions/sentiment-classifier-choice.md.
anthropic==1.3.0     # sentiment classification (6.1) + topic tagging (6.5)
fastapi==0.135.3     # the Phase 7 API layer — see app/api/
uvicorn==0.44.0      # ASGI server: `uvicorn app.api.main:app`
